# Memory Log - 2026-03-04

## Heartbeat — 16:14 AEDT

### CI Status
- **claw-forge:** Fixed lint error in `tests/test_e2e_gaps.py` (removed unused `patch` and `extract_code_blocks` imports)
- Pushed commit `daf0bfe` → `9731fcd` (after rebase)
- CI run 22656112001 in progress (was failing on lint before fix)

### RSI Health
- Score: 0.101 (critical, below 0.3 threshold)
- Ran RSI cycle: 5 proposals generated, 3 auto-approved, deployment issue (already deployed)
- Top issues: tool_error (57x in tool_call), context_loss (55x), rate_limit (11x)
- 223 outcomes logged, 26% success rate

### Active Tasks
- None in-flight

### Next Actions
- Monitor CI run 22656112001 for completion
- Consider addressing tool_error pattern (needs mock imports in subagent tasks)

## Pre-compaction flush (2026-03-04 16:28 AEDT)

### Active Project: claw-forge (clawinfra/claw-forge)
- **Repo:** `/tmp/claw-forge` (local dev checkout)
- **Latest commit pushed:** `63da8ea` on main
- **PyPI releases done:** v0.1.0a45, v0.1.0a46

### Work done this session
1. Fixed CI test coverage 89.94% → 90.52% (1188 tests)
2. Fixed WebSocket double-close bug in `proxy_ws` (cli.py:1465)
3. Fixed `--config` path resolution relative to `--project` dir
4. Fixed SIGCHLD/returncode-255 race with `_cli_semaphore = asyncio.Semaphore(1)`
5. Fixed `output_parser.py`: extensionless paths rejected as filenames
6. Released v0.1.0a45, v0.1.0a46 to PyPI
7. Added 16 regression tests in `tests/test_e2e_gaps.py`
8. Fixed state service project mismatch detection:
   - Added `/info` endpoint to `state/service.py` (returns project_path, database_url)
   - Added `/shutdown` endpoint for graceful restart
   - Modified `_ensure_state_service()` in `cli.py` to verify project match via `/info`, restart if wrong
   - Commit: `63da8ea`

### Current issue being worked on
- Browser UI: `http://localhost:5173/api/sessions/{id}` returns 404
- **Root cause:** State service on port 8420 was serving different project's DB
- **Fix:** committed and pushed (`63da8ea`)
- **Coverage:** 89.89% after adding service.py endpoints — need ~2 more test lines covered

### Coverage gap (89.89%, need 90%)
- `claw_forge/state/service.py` at 89%
- New tests added: `test_info_endpoint_returns_project_path`, `test_shutdown_endpoint_returns_status` in `test_additional_coverage.py`
- New tests: `TestEnsureStateService::test_returns_false_when_already_running`, `test_restarts_when_wrong_project` in `test_cli_commands.py`
- CI will show final coverage result

### Key files
- `/tmp/claw-forge/claw_forge/cli.py` — `_ensure_state_service()` enhanced
- `/tmp/claw-forge/claw_forge/state/service.py` — added `/info` + `/shutdown`
- `/tmp/claw-forge/tests/test_cli_commands.py` — TestEnsureStateService (3 tests)
- `/tmp/claw-forge/tests/test_additional_coverage.py` — appended 2 new endpoint tests
- `/tmp/claw-forge/pyproject.toml` — coverage exclude_lines config

### Next steps
- Wait for CI to confirm coverage passes (or add 1-2 more tests)
- Possibly release v0.1.0a47 with state service project fix
- Test the full e2e flow: `claw-forge ui` switching between projects

## Pre-compaction context save (19:19 AEDT)

### Active Task: claw-forge _ensure_state_service edge case fixes
- Repo: `/tmp/claw-forge/` (clawinfra/claw-forge)
- Latest PyPI: `v0.1.0a50`
- **What was done this session:**
  - Rewrote `_ensure_state_service()` in `claw_forge/cli.py`
  - Added `_wait_for_port()` helper (raises RuntimeError on timeout)
  - Added `_wait_for_port_free()` helper (polls up to 5s after /shutdown)
  - Fixed log_f fd leak (scoped with `with open(...)`)
  - Guard empty project_path from /info endpoint
  - Fixed ruff SIM105 lint error

- **Two tests still failing:**
  1. `test_auto_starts_when_port_free` (line ~750 in test_cli_commands.py)
  2. `test_restarts_when_wrong_project` (line ~788 in test_cli_commands.py)
  - Root cause: Tests mock Popen but no real service starts, so `_wait_for_port` 
    (which calls `socket.create_connection` in a loop using `time.monotonic`) 
    times out after 10s and raises RuntimeError
  - Fix needed: Also mock `socket.create_connection` and `time.monotonic`
  - Pattern for test_auto_starts_when_port_free:
    ```python
    patch("time.monotonic", side_effect=[0.0, 0.0, 100.0])
    patch("socket.create_connection", side_effect=[OSError("port free"), mock_conn])
    ```
  - NOT yet verified to pass — was in-flight when compaction triggered

- **Next steps:**
  1. Fix test_restarts_when_wrong_project with same pattern
  2. Run both failing tests to verify
  3. Full test suite (target ≥90% coverage, currently 90.39%)
  4. Bump version to v0.1.0a51 in pyproject.toml
  5. Build and release to PyPI

### Claude Code CLI
- Installed at `~/.local/bin/claude` (v2.1.66)
- OAuth token in gateway env `CLAUDE_CODE_OAUTH_TOKEN`
- Wrapper at `/home/bowen/.openclaw/workspace/scripts/claude_code.sh`

### RSI health
- Was 0.101 (critical) — root cause: 8 cron jobs using sessions_send in isolated sessions
- All 8 crons patched to use message(action='send', channel='telegram')
- Health score should be recovering

### Pre-push gate
- Added NON-NEGOTIABLE rule to HEARTBEAT.md: run full tests + lint before any push


### Pre-compaction flush (2026-03-04 21:09 AEDT)

**Active task:** claw-forge test fix + v0.1.0a51 release
- Repo: `/tmp/claw-forge`
- 111 tests passing, coverage ~90.39%
- Two failing tests need fix:
  1. `test_auto_starts_when_port_free` — mock `socket.create_connection` + `time.monotonic`
  2. `test_restarts_when_wrong_project` — same pattern
  - Fix pattern: `patch("time.monotonic", side_effect=[0.0, 0.0, 100.0])` + `patch("socket.create_connection", side_effect=[OSError("port free"), mock_conn])`
- After fix: full suite run, bump version to v0.1.0a51 in pyproject.toml, build + release to PyPI
- GitHub: https://github.com/clawinfra/claw-forge

**Claude Code CLI:** `~/.local/bin/claude` (v2.1.66), OAuth token in gateway env

## Heartbeat — 21:15 AEDT

### CI Status
- **claw-forge:** Clean (checked via gh CLI, 0 recent failures)
- **clawinfra org:** All repos green

### RSI Health
- Score: 0.095 (critical, below 0.3 threshold)
- Ran RSI cycle: 5 proposals generated, 1 auto-approved
- Top issues: tool_error (76x in tool_call, 100% failure), tool_error (59x), context_loss (57x)
- 244 outcomes logged, 25% success rate, avg quality 1.87/5
- **CRITICAL:** RSI health is severely degraded — needs attention

### Active Tasks
- claw-forge test fix + v0.1.0a51 release (in-flight from 21:09)
  - Two failing tests need `socket.create_connection` + `time.monotonic` mocks
  - Coverage at 90.39%, target ≥90%

### Notes
- Email credential file not found (expected — using gh CLI for CI checks)
- Heartbeat state updated: rsi, github_ci, email all checked at 21:15

