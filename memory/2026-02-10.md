# 2026-02-10 (Tuesday)

## New Models Tested
- **google-antigravity/gemini-3-pro-high** (alias: Antigravity-Gemini3) — working via sub-agent
- **google-gemini-cli/gemini-3-pro-preview** (alias: Gemini3-Pro) — working via sub-agent
- Both responded correctly, ~5k tokens each

## Intelligent Router Updated
- Added Gemini models to `intelligent-router` skill:
  - **COMPLEX**: Added `google-gemini-cli/gemini-3-pro-preview`
  - **CRITICAL**: Added `google-antigravity/gemini-3-pro-high`
- Bowen wants to make router more generic (cover all frontier + open-source LLMs) and republish to ClawHub

## Market Monitor Skill (TODO)
- Bowen wants a new `market-monitor` skill (NOT llm-monitor — I misunderstood initially)
- Should aggregate from: social media (Twitter KOLs), forums (Reddit), news sites, prediction markets
- Mistakenly created `skills/llm-monitor/` — needs cleanup/deletion

## Rate Limits & Failover
- Anthropic Opus 4.6 hitting 429 rate limits (many errors in logs around 02:13-02:43 UTC)
- Gateway failing over to proxy keys (currently on `anthropic-proxy-1/claude-opus-4-6`)
- Same Opus model, different API key pipe

## Token Consumption Analysis
- Cron jobs (GLM-4.7): ~$0.02/run (~$0.50/M tokens)
- Same tasks on Opus would cost ~$0.60/run (30x more)
- Router saving ~95% on background task costs

## Tiered Memory — Fixes Applied & Published

### v1.1.0 Fixes
1. **Cold tier fixed**: Added `_normalize_db_url()` — converts `libsql://` → `https://` for Turso HTTP API
2. **Hot tier tightened**: Reduced from 5KB → 4KB (HOT_MAX_BYTES = 4096) for headroom
3. **Cold archival enabled**: `evict_expired()` now returns evicted facts list; `consolidate()` archives them to Turso
4. **Consolidation cron updated**: Now passes Turso credentials for cold archival

### v1.2.0 Cloud-First
1. **Store dual-write**: `store` command writes to both warm JSON + cold Turso simultaneously
2. **Hot-state cloud sync**: `update_hot_state()` critical-syncs to Turso after every update
3. **New Turso table**: `hot_state` table for disaster recovery
4. **New function**: `cold_restore_hot_state()` — rebuilds hot-state from cloud
5. **New function**: `cold_sync_hot_state()` — pushes hot-state to cloud
6. **CLI args**: `--db-url` and `--auth-token` added to `store` and `hot-state` commands
7. **Architecture aligned**: Per EvoClaw CLOUD-SYNC.md "device is terminal, soul lives in cloud"

### Configuration Update
- **Turso DB**: Updated to `agentmemory-bowen31337.aws-ap-northeast-1.turso.io` (Dedicated agent memory DB)
- **Token**: Updated and encrypted in `memory/encrypted/turso-credentials.json.enc`
- **Tables**: Initialized `cold_memories` and `hot_state` in new DB

### Published
- `tiered-memory@1.1.0` → ClawHub (cold fix, hot limit, archival)
- `tiered-memory@1.2.0` → ClawHub (cloud-first dual-write, hot-state sync, disaster recovery)

### Current Stats (post-fix)
- **Hot**: 3,917 / 4,096 bytes (95.6%)
- **Warm**: 25 facts, ~8KB/50KB
- **Cold**: Connected to new DB, initialized
- **Tree**: 20 nodes

## Experiments & Deployments

### Evo-Lens (Visual Identity)
- **Installed on Pi** (`192.168.99.25`) at `~/.evoclaw/skills/evo-lens/`.
- **Backend**: Initially tried Fal.ai (failed: exhausted balance).
- **Pivot**: Switching to self-hosted ComfyUI on local GPU server (`10.0.0.44`).
- **GPU Server (10.0.0.44)**:
  - 3x GPUs: RTX 3090 (24GB), 3080 (10GB), 2070S (8GB).
  - Setup: Installed `uv`, PyTorch, ComfyUI.
  - Workers: Configured to run 3 instances on ports 8188, 8189, 8190.
  - Model: SDXL Turbo (~6.9GB) downloaded (verified).
  - **Access**: Exposed via `socat` proxy on Orchestrator (`192.168.99.44:8188` -> `10.0.0.44:8188`) to bridge networks.
- **Pi Integration**:
  - Script deployed to Pi: `evo-lens-comfy.sh`.
  - Configured to use local `COMFY_URL` (`http://192.168.99.44:8188`).
  - Pending: Verify image generation.

### Documentation Updates
- **Repo**: `clawinfra/evoclaw`
- **Updated Docs**:
  - `ROADMAP.md`: Included Tiered Memory (Phase 1b) and Skills System (Phase 2b).
  - `SKILLS-SYSTEM.md`: New architecture doc for modular skills.
- **Pull Request**: Created and merged PR #1 to `main`.

## Build Status
- **EvoClaw**: Failing CI (docs/linting issues likely).
- **Action**: Delegated fix to sub-agent.

## Cron
- **KOL Monitor**: Ran hourly from 1:21 PM to 10:22 PM. No market-moving tweets.
- **Crypto Price**: BTC dropped ~2.5% to $69.4k around 3pm, stabilized around $68.9k. No urgent alerts.

## TODO (from this session)
- [ ] Verify Evo-Lens image generation on Pi
- [ ] Make `intelligent-router` generic (all frontier + open-source LLMs) and republish to ClawHub
- [ ] Create `market-monitor` skill (social, forums, news, prediction markets)
- [ ] Delete mistaken `skills/llm-monitor/` directory

## Tiered Memory Fixes (22:15 AEDT)
- Hot tier trimmed: 95% → 89% (top 20 → top 10 warm facts in MEMORY.md rebuild)
- Added COLD_ARCHIVE_DAYS=2: facts older than 48h auto-archive to Turso cold tier
- Dual-residence: archived facts stay in warm until hard eviction at 30 days
- Cold tier was empty because eviction only triggered at 30 days — now fixed
- Committed: abbed45

## Intelligent Router Overhaul (22:13 AEDT)
- Spawned Sonnet 4.5 sub-agent to make intelligent-router ClawHub-ready
- Changes: provider-agnostic SKILL.md, config.json template, scripts/router.py CLI, README.md
- Remove hardcoded model aliases, add dynamic model registry pattern
- Status: in progress (sub-agent running)

## Skills Published to ClawHub (22:20 AEDT)
- **intelligent-router@2.0.0** published — provider-agnostic, config.json, router.py CLI, README
- **tiered-memory@1.3.0** published — cold archival fix, hot trimming, README
- Commits: a6fd5c5 (router), f3476e2 (tiered-memory)

## Image Generator Health Check (22:28 AEDT)
- ComfyUI running on GPU server 10.0.0.44:8188 (v0.12.3, RTX 3090, PyTorch 2.5.1+cu121)
- Only model: sd_xl_turbo_1.0_fp16.safetensors
- Test gen successful: 512x512 in ~12s (4 steps euler_ancestral)
- Pi (192.168.99.25) SSH access denied — no key configured, no stored credentials
- Bowen asked to configure SSH key access to Pi — waiting for credentials

## GitHub Notifications (22:28 AEDT)
- 31 CI failures on clawinfra/evoclaw (all ci_activity) — needs investigation
- No Twitter mentions of @AlexChen31337

## Tiered Memory v2.0.0 Published (23:21 AEDT)
- Full EvoClaw architecture alignment: LLM tree search, distillation engine, critical sync, multi-agent, metrics
- New scripts: distiller.py (3-stage compression), tree_search.py (LLM reasoning + keyword fallback)
- `retrieve` now finds Pi password as top result — the exact failure case that exposed the gap
- Published to ClawHub: tiered-memory@2.0.0
- Commit: e680d7b

## Evo-Lens ComfyUI Script (23:13 AEDT)
- Wrote evo-lens-comfy.sh — replaces old Fal.ai script with local ComfyUI backend
- Deployed to Pi at ~/.evoclaw/skills/evo-lens/scripts/evo-lens-comfy.sh
- Pi can't reach GPU server directly (192.168.99.44:8188 blocked, only 10.0.0.44:8188 works)
- Used SSH reverse tunnel as workaround: ssh -f -N -R 8188:10.0.0.44:8188 admin@192.168.99.25
- Previous socat bridge (192.168.99.44:8188 → 10.0.0.44:8188) is no longer running
- Bowen mentioned nginx reverse proxy on Dell — but nginx is NOT installed on Dell
- Nginx was only set up on Hetzner VPS for ClawChain, not on Dell
- TODO: Set up persistent proxy (nginx or socat) for Pi→ComfyUI access

## Pi SSH Access Fixed (22:57 AEDT)
- Password found in warm-memory.json: admin@192.168.99.25 pw 8JJ3yKNrmCoHo90F
- SSH key deployed: ~/.ssh/id_ed25519_alexchen → Pi authorized_keys
- Key auth confirmed working
- LESSON: Always use `memory_cli.py retrieve` first, not ad-hoc grep

## Intelligent Router v2.0.0 Published (23:20 AEDT)  
- Already noted above, commit a6fd5c5

## CI Failures Noticed
- 31 ci_activity notifications on clawinfra/evoclaw — all CI workflow failures
- Not yet investigated

## EvoClaw Orchestrator Fixed (23:52 AEDT)
- **Go bug**: `findProvider()` was a stub — returned first provider, didn't parse `provider/model` format
- **Fix**: Added `findProvider` parsing + `stripProvider()` helper + `strings` import
- Built patched binary at /tmp/evoclaw-patched (Go 1.23.6)
- Config copied to /tmp/evoclaw.json (root FS is read-only)
  - Fixed: Turso DB URL → agentmemory-bowen31337.aws-ap-northeast-1.turso.io
  - Fixed: agent name = Bloop, model = ollama/qwen2.5:1.5b
  - Fixed: dataDir → /tmp/evoclaw-data (writable)
  - Fixed: intelligentRouter MEDIUM tier → zhipu-1/glm-4.7
- **Result**: Bloop responded! Pi → MQTT → Orchestrator → Ollama → 1.7s, 39 tokens
- Root FS mounted read-only: /home/bowen/evoclaw/ not writable, only /tmp, /media/DATA/clawd, ~/.openclaw are rw
- Turso cloud sync still has JSON parse error on device registration (non-critical)

## Evo-Lens Selfie Test (00:00 AEDT)  
- Generated Bloop selfie from Pi via SSH tunnel → ComfyUI
- SDXL Turbo: 1s generation, decent but limited photorealism
- Bowen requested realistic Asian female — generated but quality limited by SDXL Turbo
- TODO: Consider adding better model (SDXL base, Flux) to ComfyUI for photorealism

## Network Topology Notes
- Pi (192.168.99.25) can ping GPU server at 192.168.99.44 but port 8188 blocked
- GPU server ComfyUI only accessible on 10.0.0.44:8188 (dual NIC, firewall blocking 192.168.99.x)
- Workaround: SSH reverse tunnel from Dell → Pi (-R 8188:10.0.0.44:8188)
- Tunnel not persistent — dies on SSH disconnect
- TODO: Either open firewall on GPU server or set up persistent tunnel (autossh)
