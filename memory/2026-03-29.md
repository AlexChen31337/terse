# 2026-03-29 Daily Notes

## Daily Book Pipeline
- **Book:** "The AI Product Manager: Build Better Products Faster with Artificial Intelligence"
- **Status:** Written (8908 words), PDF generated, cover generated, pushed to GitHub
- **GitHub:** `bowen31337/mbd-book-ideas` → `2026-03-29_The-AI-Product-Manager/`
- **Cover:** https://files.catbox.moe/qp92jr.png
- **Payhip:** ⚠️ Publish attempted but **login failed** — credentials may have changed (email/password auth on payhip.com/auth/login timed out on input fields). Needs manual re-publish or credential refresh.
- **Sub-agent timed out** at 30min — spent 18:51 on first ComfyUI model load (cold start), then subsequent covers generated in ~3s each. Book content was already written before timeout.

## Issues
- **Payhip login broken** — `input[name="email"]` selector no longer works on payhip.com login page. May need to update publish script selectors. The `input[placeholder*="email"]` worked but actual login POST failed (stayed on /auth/login after submit).
- **Gateway flaky** — sessions_spawn and sessions_list returning gateway timeouts/closures intermittently.
- **Memory search unavailable** — embedding provider error, memory_search returning disabled=true.
