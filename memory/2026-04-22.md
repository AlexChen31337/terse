# 2026-04-22 Daily Notes

## Credentials
- **GitHub PAT (bowen31337):** 90-day fine-grained PAT stored at `~/.gh-bowen31337-token` (mode 600). Works for `bowen31337/*` repos only — lacks clawinfra org write access. Use AlexChen31337 OAuth for clawinfra merges.
- Previous PAT (long-lived >366 days) rejected by clawinfra org policy.

## Decisions
- All 3 claw-chain security PRs merged (#102, #103, #104) using AlexChen31337 token
- PR #106 (RUSTSEC audit cleanup) created by Sonnet sub-agent — all 12 advisories addressed, `cargo audit` clean. **Awaiting Bowen's merge approval.**
- Seek.com.au nightly job scraper cron recreated (3:30 AM AEST, Sonnet 4.6) — original was lost
- ClawMemory plugin is the memory system (not built-in memory_search which is broken due to missing node-llama-cpp)
- Active model confirmed: `openrouter/openrouter/elephant-alpha`
- **@unoclawd HOSTILE flag CLEARED (12:58 PM)** — Bowen migrated unoclawd to Opus, actively repairing. MEMORY.md updated.
- **Qwen 3.6-35B-A3B-Q6_K_P confirmed live on GPU server (10.0.0.30)** — tensor-split 24/10/8 across 3090+3080+2070S, thinking model, ~85 tok/s effective. Provider registered as `gpu-server-35b` / `Qwen3.6-35B-A3B-Q6_K_P.gguf`.
- **SmartShift advisor cron fixed** — added `.env` auto-loader to `advisor_v2.py`, removed chained exports that tripped exec preflight guard. Verified: 49 forward intervals, strategy=hold, expected earn 44¢.
- **Workspace perms tightened** — `/media/DATA/.openclaw/workspace` → 755 bowen:bowen.

## Cron Status (37 jobs)
- 25 healthy, 6 failing (timeouts), 12 disabled

## 17:20 GPU Server Miner Correction
- Bowen noticed 3090 at 100% SM, 333W while llama-server slot was idle
- `nvidia-smi pmon` revealed: `/home/peter/Documents/miner/miner` (pid 105526) was active on all 3 GPUs
- HA had re-launched the miner autonomously (SoC threshold hit)
- **My earlier 15:00 report "GPU server not mining" was WRONG** — only checked `pgrep` for t-rex/gminer/etc brand names, missed generic `miner` basename
- Stopped miner via `pkill -x miner` (note: `pkill -f /full/path` fails because pattern also matches the invoking shell wrapper)
- GPUs now exclusively holding Qwen 3.6 weights; power draw 333W→102W on 3090
- **Lesson:** always check `nvidia-smi pmon` + `--query-compute-apps=process_name` for generic names, not just miner brand strings
- **Lesson:** `pkill -x <basename>` or kill-by-PID is safer than `pkill -f <path>` for processes whose path appears in parent shell args

## Tool-Call Format Lesson
- Emitted raw `<parameter>...