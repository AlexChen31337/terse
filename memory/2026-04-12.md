# 2026-04-12 Daily Notes

## qwen35b-opus Chat UI Fix (00:00)
- **Issue:** `gpu-server-35b/qwen35b-opus` was invisible in Chat UI — `chatUiLabel` was empty (schema: empty = hidden)
- **Second issue:** llama-server running with `-c 8192`, OpenClaw blocks models with ctx < 16000 minimum
- **Fix:** Restarted llama-server with `-c 16384`, set `chatUiLabel = "Qwen 35B Opus (GPU Server)"`
- **VRAM:** 20GB/24.5GB on RTX 3090 after fix
- **Config:** `contextWindow: 16384, maxTokens: 4096, chatUiLabel: "Qwen 35B Opus (GPU Server)"`
- **Config file location:** `/home/bowen/.openclaw/workspace/skills/intelligent-router/config.json` (gateway patches via openclaw config tool)
- **Note:** `config.patch` raw param must be a JSON string not object — gateway auto-deserializes, this was causing validation failures

## Network Topology (confirmed Apr 12)
- **Dell XPS (this machine):**
  - Ethernet (enp0s31f6): `10.0.0.4` — NOT reachable from GPU server
  - WiFi (wlo1): `10.0.0.18` — REACHABLE from GPU server
- **GPU server:** `10.0.0.30` (ethernet), also has wifi adapter `wlxd03745e05569`
- **HA URL from GPU server:** `http://10.0.0.18:8123` (not 10.0.0.4!)
- **HA URL from HA container (Docker):** `localhost:8123` or `http://10.0.0.4:8123`

## HA Miner - miner_scheduler Investigation (10:00)
### Root cause of `failed` state
- The existing `miner_scheduler.sh` is the **sunwait-based solar script** (NOT the HA-querying one I wrote)
- It uses `sunwait` for sunrise/sunset timing, runs all day, starts miner at `mine_rvn.sh`
- Was running fine for 6+ days, then got killed at 19:18 Apr 11 by HA automation `shell_command.miner_stop`
- HA automation fired every 2 min seeing SOC < 90% → systemctl stop → service failed → Restart=always tried to restart but HA kept killing it

### Decision: Use HA automation as sole controller
- **`miner_scheduler.service` — masked** (disabled + masked, cannot restart)
- **New `miner_ctl.sh`** created at `/home/peter/Documents/miner/miner_ctl.sh`
  - `start` → nohup mine_rvn.sh, track PID in /tmp/ha_miner.pid
  - `stop` → kill process group + pkill kawpow
  - `status` → active/inactive
- **HA shell_commands updated:**
  - `miner_start`: SSH → `miner_ctl.sh start`
  - `miner_stop`: SSH → `miner_ctl.sh stop`
  - `miner_status`: SSH → `miner_ctl.sh status`

### Miner details
- **Binary:** `/home/peter/Documents/miner/miner` (NOT t-rex at /opt/t-rex)
- **Script:** `/home/peter/Documents/miner/mine_rvn.sh`
- **Algo:** kawpow (RVN)
- **Pool:** rvn.2miners.com:6060
- **Wallet:** RDj3qggPLQM3EsP3fXdm7h72xPBrsZUQyV.rigone
- **Devices:** 0,1,2 (all 3 GPUs)

## HA Miner - Configurable SOC Thresholds (10:15)
- **Added `input_number` helpers:**
  - `input_number.miner_soc_start` = **80%** (default, range 50-100, step 5, slider)
  - `input_number.miner_soc_stop` = **70%** (default, range 20-95, step 5, slider)
- **Template sensors updated** to read from these helpers (previously hardcoded 95%/90%)
- **Dashboard:** Sliders added to ⛏️ GPU Miner control card
- **Verified:** Both entities live, `miner_should_run = False` (SOC 49% < 80%)

## Broken custom_components/input_number Fix (10:45)
- **Problem:** `input_number` entities kept showing MISSING after restarts
- **Root cause:** A broken custom integration at `/config/custom_components/input_number/` was overriding the built-in `input_number` component. HA logged: "We found a custom integration input_number... Not loading component."
- **Fix:** Removed from HOST path `/home/bowen/homeassistant/config/custom_components/input_number/` (not just container — must use host volume mount)
- **Also:** The `input_number:` section was never actually written to configuration.yaml (Python string replacement failed on whitespace mismatch). Fixed with `sed -i` directly on host file.
- **Remaining custom_components:** `hacs`, `solplanet` (both legitimate)

## GPU Stats Polling Fix (11:45)
- **Problem:** `sensor.miner_service_status` showed "failed" and `sensor.miner_total_power` showed 52.6W (stale) while miner was actually running
- **Root cause:** `/home/bowen/ha-smartshift/scripts/poll_gpu_stats.sh` was **missing** — never created. The user systemd timer `gpu-stats-poll.timer` fired every 60s but the service failed with `status=203/EXEC`
- **Fix:** Created `poll_gpu_stats.sh` — SSHes to GPU server, runs `miner_ctl.sh status` + `nvidia-smi`, writes `.gpu_stats.json`
- **Also added:** `sensor.miner_last_updated` — shows friendly datetime of last poll in dashboard
- **Timer:** `~/.config/systemd/user/gpu-stats-poll.timer` (user-level, 60s interval)

## Llama-Server Auto-Toggle (12:00)
- **Problem:** GPU0 (3090) couldn't mine — `llama-server` (9B, port 8080) + `llama-qwen35b` (35B, port 8081) used ~24GB VRAM on GPU0
- **Solution:** Updated `miner_ctl.sh` to auto-toggle:
  - `start` → stops `llama-server` + `llama-qwen35b` services → starts miner on all 3 GPUs
  - `stop` → stops miner → restarts both llama services
- **Result:** All 3 GPUs mining: 23.23 + 28.54 + 16.64 = **68.41 MH/s** total
- **Services on GPU server:**
  - `llama-server.service` — Qwen 9B Opus (port 8080, ~6GB VRAM on GPU0)
  - `llama-qwen35b.service` — Qwen 35B Opus IQ2_XXS (port 8081, ~18GB VRAM on GPU0, ngl=27)
  - Both use `Restart=on-failure` but `miner_ctl.sh` uses `sudo systemctl stop` which is clean

## GPU Server Services Reference
| Service | Binary | Model | Port | GPU | VRAM |
|---------|--------|-------|------|-----|------|
| `llama-server` | `/data/llama.cpp/build/bin/llama-server` | qwen9b-opus-Q5_K_M | 8080 | GPU0 | ~6GB |
| `llama-qwen35b` | `/data2/llama.cpp/build/bin/llama-server` | qwen35b-opus-IQ2_XXS | 8081 | GPU0 | ~18GB |
| `miner_scheduler` | `/usr/local/bin/miner_scheduler.sh` | — | — | — | **MASKED** |

## Current Miner State (12:02)
- `input_boolean.miner_enabled` = **on**
- `input_number.miner_soc_start` = **70%** (Bowen changed from 80%)
- `input_number.miner_soc_stop` = **50%** (Bowen changed from 70%)
- `sensor.battery_soc` = **~77%** (charging)
- `sensor.miner_should_run` = **True** (77% > 70%)
- All 3 GPUs mining at **68.41 MH/s** total
- Both llama services **stopped** (auto-toggled by miner_ctl.sh)
- `miner_scheduler.service` = **masked** on GPU server

## HA Config Editing Lessons (CRITICAL)
- **Always edit HOST path** `/home/bowen/homeassistant/config/configuration.yaml` — container-only edits get overwritten on restart
- **For complex edits:** use `docker exec homeassistant python3 -c "..."` to edit `/config/configuration.yaml` inside container (avoids host permission issues with sed)
- **Dashboard edits:** `/home/bowen/homeassistant/config/.storage/lovelace.lovelace` (host path)
- **Custom components:** Must remove from HOST at `/home/bowen/homeassistant/config/custom_components/`

## HA Container Volume Mounts
- `/home/bowen/homeassistant/config` → `/config`
- `/home/bowen/ha-smartshift` → `/ha-smartshift`

## Pending
- CCP thinking support (Option A) — still not implemented
- EvoClaw v0.6.1 — PRs #22+#23 pending
- ClawChain PR #55 — pending
- Commit updated miner config + poll_gpu_stats.sh to bowen31337/ha-smartshift repo

## Dreaming Enabled (18:49)
- **Config:** `plugins.entries.memory-core.config.dreaming.enabled = true`
- **Schedule:** `0 3 * * *` (3am AEDT nightly)
- **Cron ID:** `managed:dreaming-sweep`
- **First run:** 13 Apr 2026, 3:00 AM AEDT
- **Input:** 68 daily note files (2026-02-04 to 2026-04-12)
- **Output:** DREAMS.md + memory/.dreams/ state
- **⚠️ CLI issue:** `openclaw memory rem-backfill` hangs — CLI tries to boot a second gateway instead of connecting to running one (port conflict). All `openclaw memory *` commands affected. Filed for next update.

## New Features Considered (19:18)
- ✅ Dreaming — enabled
- ✅ Compaction memory flush — already configured
- ✅ Context pruning (cache-ttl) — already configured  
- 🔲 Grounded backfill — blocked by CLI issue, first sweep will naturally ingest all daily notes
- 🔲 Sub-agent exec sandbox tightening — deferred
- 🔲 Canvas/A2UI dashboards — nice-to-have
- 🔲 WhatsApp re-enable — deferred
