# 2026-04-04 — GPU Server llama.cpp Migration + Skills

## GPU Server (peter@10.0.0.30)
- **Ollama removed** (stopped, disabled, binary deleted)
- **llama.cpp built** from ggml-org latest with CUDA + FA at `/data/llama.cpp/build/bin/`
  - `llama-server`, `llama-quantize` confirmed working
  - CUDA sees all 3 GPUs (3090, 3080, 2070S)
- **Systemd service** installed: `/etc/systemd/system/llama-server.service`
  - Port 8081, CUDA_VISIBLE_DEVICES=2 (2070S), `-ngl 99 --cache-type-k q8_0 --cache-type-v q8_0 -fa on -c 4096`
  - Enabled (auto-start on boot)
- **Watcher script** running (PID 25503): waits for safetensors download → convert → quantize → start service
- **Model download**: 9B Opus-distilled safetensors downloading directly on server from HuggingFace
  - Shard 1: 2.2GB/~4.5GB, total 4 shards (~18GB)
  - Slow: GPU server internet bandwidth limited

## Knowledge Base Skill
- Wired into workspace at `skills/knowledge-base/` with symlinks to `/tmp/llm-knowledge-base/`
- Repo: https://github.com/AlexChen31337/llm-knowledge-base

## Skills Repo
- Sub-agent spawned to create `AlexChen31337/openclaw-skills` with custom skills
- Consolidates: knowledge-base, alphastrike, autoresearch, bird, agent-wal, etc.

## Autoresearch Hardware
- GPU server IP changed: 10.0.0.44 → 10.0.0.30 (same machine, new IP)

## TODO
- [ ] Check watcher completion → llama-server live on port 8081
- [ ] Test inference from XPS: `curl http://10.0.0.30:8081/v1/chat/completions`
- [ ] Wire into OpenClaw as remote inference endpoint
- [ ] Skills repo: confirm push completed

## GPU Server Multi-Model Setup (00:27 AEDT)
- HTTP server on XPS (192.168.99.44:9999) serving GGUFs to GPU server
- Two parallel wget downloads running on GPU server (peter@10.0.0.30):
  - 9B Q5_K_M (6.1GB) → /data/models/qwen9b-opus-Q5_K_M.gguf — GPU 2 (2070S), port 8081
  - 35B-A3B MoE Q3_K_M (16GB) → /data/models/Qwen3.5-35B-A3B-Opus-Distilled-Q3_K_M.gguf — GPU 0 (3090), port 8080
- Download speed: ~800KB/s (WiFi LAN), ETA 2-3.5h
- One-shot cron at 04:30 AEDT will install services + start servers + update config
- XPS llama-server already running: port 8081, 56 tok/s, 4096 ctx
- Ollama fully removed from both XPS and GPU server
- Anthropic OAuth health monitor cron set up (every 15min, Telegram only)
- Claude Code CLI confirmed working as independent fallback (separate Pro quota)
