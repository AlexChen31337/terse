
## Gateway Restart Loop Fix — 2026-04-13 16:20–17:00

### Root Cause Found
- **Session supervisor** (`openclaw-session-supervisor.py`) was restarting gateway every 5 minutes
- Subagent `1edbe022` (FTS5 clawmemory) at 82% pressure (163K tokens) triggered AUTO-COMPACT
- Supervisor stopped gateway, tried truncating, found only 169 lines (< 200 keep threshold), skipped
- Never recorded cooldown → triggered again 5 min later → infinite restart loop
- **This killed in-flight messages** — gateway stop mid-response = dropped replies

### Fix Applied to `/media/DATA/development/bots/openclaw-session-supervisor.py`
1. **Pre-check file size BEFORE stopping gateway** — if truncation would be skipped, don't stop gateway at all
2. **Record cooldown on skip** — `write_state(session_key, "compact")` even when truncation skipped
3. `do_truncate()` returns `"skip"` instead of `False` when truncation unnecessary
4. Syntax verified ✅, dry-run confirmed fix works (session evaluates as "healthy")

### Issue Still Occurring
- Bowen reports messages still not being replied to at 17:38
- Previous fix may not be sufficient — investigate further
- Possible causes: gateway still restarting from other triggers, or message processing issue

### Pending Investigation
- Why messages are still being dropped after supervisor fix
- Need to check if gateway has restarted since the fix was applied
- Check if there are other restart triggers beyond the supervisor

## Gateway Stability Fix — 2026-04-13 17:00–18:00

### Problem: Messages Dropped Due to Gateway Restart Loop
- Bowen reported multiple messages not replied to throughout the day
- Root cause: `openclaw-session-supervisor.py` was restarting gateway every 5 minutes

### Root Cause Chain
1. Subagent `1edbe022` (FTS5 clawmemory) at 82% pressure triggered AUTO-COMPACT every 5 min
2. Supervisor stopped gateway, found too few lines to truncate, skipped, restarted — infinite loop
3. Fixed subagent issue (pre-check before stop + record cooldown on skip) — but main session ALSO triggered compaction at 17:24 (62% pressure)
4. Gateway restart killed in-flight replies

### Fixes Applied to `/media/DATA/development/bots/openclaw-session-supervisor.py`
| Setting | Before | After |
|---------|--------|-------|
| `PRESSURE_COMPACT_THRESHOLD` | 0.55 (55%) | 0.80 (80%) |
| `COMPACT_COOLDOWN_SECONDS` | 1800 (30 min) | 3600 (60 min) |
| Pre-check file size | Stopped gateway first | Check BEFORE stopping |
| Skip → record cooldown | No | Yes (prevents restart loop) |
| Active session check | None | Skip if session status=running |

### Result
- Gateway stable 15+ minutes after fix
- Main session at 45% pressure → healthy (well below 80%)
- No more restart loops confirmed via dry-run

### Key File
- Supervisor script: `/media/DATA/development/bots/openclaw-session-supervisor.py`
- State file: `/tmp/openclaw_supervisor_state.json`

## Daily Payhip Book — Failed (Rate Limit) — 20:36

- **Task:** daily-book-2026-04-13 (Cron: Gumroad Daily Book Draft)
- **Status:** ❌ Failed — all model endpoints hit 429 rate limits (resets 1am AEST)
- **MbD book:** ✅ Succeeded — 懒人理财经：当AI成为你的私人财务总监
- **Action:** Will retry Payhip book after 1am when rate limits reset
