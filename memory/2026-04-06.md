# 2026-04-06 Daily Notes

## Session Context (pre-compaction ~8:24 AM AEDT)

### Key Events This Session
1. **remote-llama provider debug** — Bowen asked why `remote-llama/Qwen3.5-35B-A3B-Opus-Distilled-Q3_K_M.gguf` failed. Root cause: `remote-llama` provider doesn't exist in config; correct alias is `gpu-server-35b/qwen35b-opus`. Dead alias needs cleanup from agents.defaults.models. Bowen approved cleanup but it wasn't completed yet.

2. **Claude Code OAuth Plugin article** — Bowen requested articles about `openclaw-plugin-claude-code` (the plugin that routes Claude models via OAuth token from Claude Code CLI):
   - **MbD version = CHINESE** (correction from Bowen — I initially wrote it in English)
   - **Blog/Substack version = ENGLISH**
   - Both pushed to `bowen31337/mbd-book-ideas` repo under `2026-04-05_我如何连接Claude_Code的OAuth令牌到Agent框架/`
   - Cover image generated and pushed (`cover.png`, 746KB)
   - MbD NOT published — waiting for explicit "publish to MbD" from Bowen

3. **GPU server changes:**
   - 10.0.0.44 is unreachable (no route to host)
   - "gpu1" = 10.0.0.30 (Bowen confirmed)
   - ComfyUI runs on 10.0.0.30:8188
   - llama-server was consuming 18GB on the 3090, causing ComfyUI to thrash

### Corrections
- **MbD is always Chinese version** — I wrote English initially, Bowen corrected. Remember: MbD (面包多) = Chinese platform = Chinese articles.

### Pending Tasks
- Clean up dead `remote-llama` alias from config (Bowen approved)
- MbD article ready for publish (awaiting Bowen's "publish to MbD")
- Blog/English article ready for Substack/Medium publish

### SmartShift Status (latest ~8:16 AM)
- Battery: 22% SoC, discharging -6.6kW, self-consumption mode
- Cloudy day (94% cloud, ~56 kWh solar est)
- Tomorrow better: partly cloudy, ~98 kWh
- Strategy: Conservative Hold, no export today

### AlphaStrike V3 (latest ~6:11 AM)
- All 3 pairs FLAT (BTC/ETH/SOL)
- Portfolio: 994.06 USDC, 0 open positions, -0.6% PnL

### RSI Health
- Score: 0.296 (below 0.3 threshold)
- Main issues: context_loss (5x), incomplete_task (2x)
