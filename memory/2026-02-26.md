# 2026-02-26

## Cron Job Issue: Intelligent Router Model Discovery

**Time:** 18:24 AEDT

Cron job `a8992c1f` tried to run `discover_models.py --auto-update` but the script doesn't exist.

**Actual location:** `~/.evoclaw/skills/intelligent-router/`
**Actual files:**
- `router` (bash wrapper)
- `router.py` (main routing logic)
- `spawn_helper.py` (model classification helper)

**Root cause:** Cron job was configured for a feature that hasn't been implemented yet. The intelligent-router skill exists but doesn't have a live model discovery script.

**Action needed:** Either implement `discover_models.py` or remove this cron job.

## Agent Supervision Check (19:02 AEDT)

**Quant (Trading):** ✅
- AlphaStrike: ACTIVE
- Fear & Greed: 11 (last_action: DCA_BUY)
- Run log: no run_log.jsonl file found

**Sentinel (Market Watch):** ⚠️ STALE
- Last check: 620 min ago (~10 hours)
- State file exists but hasn't been updated

**Shield (Access Control):** ✅
- Owners: 4, Approved: 0, Pending: 0

**Issues identified:**
1. FearHarvester run_log.jsonl missing — check if skill writes to different location
2. Sentinel hasn't checked in ~10 hours — likely stale session or dead cron


## Lessons Learned (Feb 26 late)

### GLM-4.7-flash inference broken on GPU server
- Model loads into GPU 0 VRAM (21.7GB) successfully
- Inference NEVER completes — timed out at 300s with num_predict:3
- GPU utilization stuck at 2-3% (not computing)
- Ollama 0.16.2, model family: glm4moelite, Q4_K_M
- TODO: try different Ollama version, or re-pull model, or test qwen2.5:7b as fallback

### Simmer BTC >$68k position — total loss
- Entry: $0.54, resolved: $0.001 → -$15.12 (-99.9%)
- Circuit breaker triggered: portfolio $6.47 vs HWM $22.99 (-71.9%)
- Root cause: 100% concentration in single binary outcome
- Fix: Add max single-position limit (25% of active capital) to risk_config.json
- Trading paused via circuit breaker until portfolio recovers or HWM resets

### session-memory hook enabled
- `openclaw hooks enable session-memory` — auto-saves context on /new and /reset
- Fixes the memory gap where conversations were lost between session resets
