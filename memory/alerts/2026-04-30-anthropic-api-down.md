# Anthropic API Down — 2026-04-30 12:00 AEDT

**Status:** ALL_API_DOWN (direct + proxy-1 both 401)
**Claude Code CLI:** Healthy ✅
**Routing:** All tasks via Claude Code CLI (`sessions_spawn runtime=acp` or `claude --print`)
**Fix needed:** `openclaw auth` to re-link OAuth tokens
**Telegram alert:** Could NOT send — TELEGRAM_BOT_TOKEN not configured. This is the 3rd time this gap has been noted.
