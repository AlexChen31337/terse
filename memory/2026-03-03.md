
## Pre-Compaction Flush — 2026-03-03 00:11 AEDT

### claw-forge Status (as of session end)
- **Repo:** https://github.com/clawinfra/claw-forge
- **Latest commit:** `6e224f2` on main
- **Local clone:** `/tmp/claw-forge`
- **Tests:** 796 passing, 90.26% coverage, mypy clean
- **Release:** v0.3.0 created — https://github.com/clawinfra/claw-forge/releases/tag/v0.3.0
- **CI:** Green (Python 3.11/3.12/3.13, `fail-fast: false`, `actions/setup-python@v5`)
- **README:** Tests badge = 796, PyPI badges live (will auto-update once published)

### PyPI Publishing — BLOCKED
- **Root cause:** Trusted publisher OIDC not configured on pypi.org for clawinfra/claw-forge
- **Fix applied:** Updated `.github/workflows/publish.yml` to use `uv publish --token ${{ secrets.PYPI_TOKEN }}`
- **Action needed:** Bowen must:
  1. Create PyPI account at https://pypi.org
  2. Generate API token (scope: entire account for first upload)
  3. Add secret `PYPI_TOKEN` to GitHub repo → Settings → Secrets → Actions
  4. Re-run the publish workflow (or create a new release)
- **Build works locally:** `uv build` produces `dist/claw_forge-0.1.0a1-py3-none-any.whl`

### Pending Next Steps for claw-forge
1. Get PYPI_TOKEN from Bowen → publish to PyPI
2. Implement `BrownfieldAnalyzer` (`claw_forge/plugins/analyzer.py`) + `claw-forge analyze` command per `/tmp/claw-forge/PLAN.md`
3. Wire `AgentSession` into orchestrator (replace bare `query()` calls)
4. UI polish subagent (10 features: xterm.js, AI panel, dagre graph, etc.)
5. v0.3.1 or v0.4.0 release after above

### Paper Trade Open Position
- **Pair:** ETH/BTC ratio
- **Entry:** 0.029454 (opened ~Mar 2, 10:21 AEDT)
- **Direction:** LONG (2x leverage)
- **SL:** 0.0275 | **TP:** 0.035
- **Current:** 0.02937 (-0.28% unrealized, -0.56% with 2x)

### MbD Blog — Next Topic
- **topic_index=3** in `memory/mbd-publisher-state.json` = "赛博朋克副业"
- Cover image generated: `/tmp/mbd_cover_cyberpunk_00001_.png` (sent to Telegram, awaiting Bowen feedback)
- Need to read blog content for agent-teams article for embedded image prompt

### Infrastructure Notes
- pypi-token.txt.enc decrypt fails with "bad magic number" — may be wrong key or corrupt
- ComfyUI running via nohup on peter@10.0.0.44 (not systemd — crashes after 7s)
- GPU server: 3 GPUs, 42GB VRAM total; ZImage models on /data SSD

