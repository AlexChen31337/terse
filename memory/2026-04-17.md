
## 2026-04-17 20:57 AEDT — Session Flush

### Bug Fix: pv_meter_raw Stale MQTT Value
- **Root cause**: `inverter_control.py` was silently skipping MQTT publish on PV meter read failure
  - File: `/home/bowen/ha-smartshift/scripts/inverter_control.py`
  - Bug: `except Exception: log.warning("PV meter read failed, skipping MQTT publish")`
  - Fix: Now publishes `0` on failure instead of skipping
- **Immediate fix**: Pushed `0` via HA MQTT API — `sensor.pv_meter_raw` now shows 0W (was stuck at 2000W since 3:28 PM)
- **MQTT topic**: `homeassistant/sensor/pv_meter_raw/state` with retain=true

### Email: Hightouch Article Resent as HTML
- First send was raw markdown — Bowen requested HTML version
- Converted via pandoc with styled CSS
- Resent to peterlu2000@gmail.com, CC bowen31337@outlook.com ✅
- Full article text saved at: /tmp/hightouch-article.md, /tmp/hightouch-article-styled.html

---
## Session Block: 2026-04-17 ~20:45–23:02 AEST

### HA / SmartShift Fixes

**PV Meter Raw stale value fix:**
- Root cause: FoxESS smart meter (`device=3`) reports phantom `meter_general.prc ~2000W` at night with no solar
- Fix: Updated `value_template` in `ha/configuration_addon.yaml` to zero out when solar DC voltage < 50V
- HA restarted to apply

**Battery export not triggering despite 9c feed-in > 7.5c threshold:**
- Root cause: Bug in `smartshift_control.py` — AI advisor `strategy: hold` veto was applied AFTER the threshold check, causing hold to override even when `earn_now >= export_threshold`
- Fix: Move the hold veto inside the `earn_now < threshold` branch — only veto when BELOW threshold
- Key log line post-fix: "AI advisor says HOLD but earn=9.53c ≥ 7.5c threshold — overriding hold" → `discharge`
- Docker container restarted, battery confirmed discharging at 4994W, exporting 4243W @ 9.53c

**HA dashboard sync (22:08):**
- Mode: Discharge / Forced Export
- Battery: 85% SOC, 4994W output
- Grid export: 4243W
- Export today: 24.91 kWh

### Memory Search / Embedding

- Local embedding model confirmed: `/data/models/embeddinggemma-300m-Q8_0.gguf` (328MB)
- Server: `localhost:8082` via llama-server (PID 2591409, `-ngl 0`)
- OpenClaw config updated: `memorySearch.provider` changed from `openai` → `local` using GGUF path
- Gateway restarted to apply — memory search should now work without OpenAI API key

### Payhip Book

- Book: "The AI Startup Playbook: From Zero to Product-Market Fit with Artificial Intelligence"
- ~8000 words, 12 chapters
- PDF at `/tmp/book_2026-04-17.pdf`
- ⚠️ Payhip publish BLOCKED by reCAPTCHA — needs manual publish or browser automation fix
- Cover: PIL fallback (GPU servers unreachable)
- Payhip login: `input[name="login"]`, password `PayhipAlex@2026!` (from memory), `--disable-blink-features=AutomationControlled`

### RUSTSEC / claw-chain

- Sonnet sub-agent spawned to fix RUSTSEC issues:
  - #96: RUSTSEC-2025-0161 — libsecp256k1 unmaintained
  - #97-98: RUSTSEC-2026-0097 — rand unsound (PR #95 open, conflicting)
  - #99-100: RUSTSEC-2026-0098/0099 — webpki name constraint bugs
- Status: sub-agent running, awaiting completion

### GPU Server / Miner

- GPU server (10.0.0.30) unreachable from this machine — cannot SSH
- HA GPU stats were stale (last updated Apr 11)
- Bowen reported miner still running at ~21:05; could not stop remotely
- No resolution confirmed — Bowen may need physical access

### AlphaStrike V3 Paper Trading (11PM)

- Equity: $1,025.20 (+2.52%)
- Win rate: 60% (3/5 closed trades)
- All signals FLAT — BTC/ETH/SOL all overbought (RSI 74–84), correctly gating new longs

### Unanswered / Pending

- Payhip book still needs publishing (reCAPTCHA blocker, browser automation failed with SIGKILL)
- RUSTSEC sub-agent result not yet confirmed
- GPU server network issue unresolved
