# 2026-04-15 Daily Notes

## Session Summary (08:45 AEDT)

### ClawChain RUSTSEC Security Fixes ✅
- **PR #95** (rand 0.8.5 → 0.9.3, RUSTSEC-2026-0097): Had CI failure + merge conflict after PR #96 landed. Rebased onto main, resolved conflict, CI green, **merged**. Closes #92, #93.
- **PR #96** (wasmtime 28.0.0 → 30.0.3, 11 RUSTSEC advisories incl critical sandbox escape #84): Full API migration in `pallets/contracts/src/wasm/` (runtime.rs + executor.rs). cargo check ✅, cargo audit ✅ 0 vulnerabilities. **merged**. Closes #81–#91, #94.
- **Result:** claw-chain `cargo audit` = **0 active advisories**. All 13 RUSTSEC issues resolved.

### ClawMemory Architecture Review
- Bowen asked to review clawmemory vs OpenClaw builtin memory for replacement.
- **Conclusion:** ClawMemory should replace builtin memory. BM25 search already works (builtin semantic search is broken — no OpenAI key). Gaps: vector search stubbed but not wired, plugin needs testing with current OpenClaw, need bulk ingest of 200+ daily notes.
- **Recommendation:** Wire up clawmemory server + OpenClaw plugin, transition from manual MEMORY.md to auto-captured facts.
- **Status:** Awaiting Bowen's go-ahead to do the integration.

### Infrastructure Updates
- **HA Pi IP changed:** `192.168.99.188` → `10.0.0.18`. Updated in TOOLS.md and warm-memory.json.
- **ClawChain repo confirmed:** `clawinfra/claw-chain` (NOT `claw-node` — earlier subagent had wrong name)

### Active/Pending Tasks
- ClawMemory ↔ OpenClaw integration (awaiting confirmation)
- EvoClaw v0.6.1 release
- ClawChain multi-validator testnet
- HA Pi at new IP 10.0.0.18 — health check needed
- RSI Loop health score 0.263 (below 0.3 threshold) — context_loss is biggest contributor

## Session Update (09:03 AEDT)

### llama-server Binary — Permanent Location
- Moved from `/tmp/llama.cpp/build/bin/llama-server` → `~/bin/llama-server` (permanent, survives reboot)
- Also copied `~/bin/llama-cli`
- **TOOLS.md should be updated** to reflect new permanent path `~/bin/llama-server`

### Embedding Model (embeddinggemma-300m)
- `/data/models/embeddinggemma-300m-Q8_0.gguf` — already downloaded on Dell XPS
- Serving on port 8082 via llama-server, 768 dimensions, confirmed working
- `qwen9b-opus` is a generation model — NOT capable of embeddings (confirmed)

### ClawMemory Embedding Gap (IMPORTANT)
- ClawMemory v0.2.0 has vector embeddings **deliberately disabled** in code
- `resolver.go`, `sqlite.go`, `search.go` all explicitly skip embedding generation
- Embedding column in DB exists but is never written to — BM25-only search
- 7,508 facts stored, 0 embeddings generated
- **Two paths discussed:**
  1. Wire embeddings into clawmemory Go code (~200-300 LOC) — proper fix
  2. Point OpenClaw's builtin memory_search at localhost:8082 instead of OpenAI — 5-min config fix
- **Awaiting Bowen's decision on which to pursue**

### Infrastructure Status
- Embedding server: ✅ localhost:8082 (embeddinggemma-300m)
- ClawMemory server: ✅ localhost:7437 (7,508 facts, BM25-only)
- OpenClaw memory_search: ❌ still broken (no provider reconfigured yet)

### Unanswered Question
- Bowen's choice: wire embeddings into clawmemory (Option 1) vs fix OpenClaw memory_search provider config (Option 2)?

## Amber API Key — RESOLVED
- New key: psk_4c5df... (from Bowen, 16:17 AEDT)
- Encrypted to memory/encrypted/amber-api-key.enc (master key)
- Saved to /home/bowen/ha-smartshift/.env
- SmartShift hourly cron (8fb10ca9) updated with key injection
- API verified: Site 01KMZ2TGWXT9ASYAK62Y1HMMCR, NMI 4311291707, active
- Advisor tested: 24.83¢/kWh, HOLD decision (inverter 10.0.0.2 still unreachable → SoC null)
- ha-smartshift advisor.py runs correctly with .env sourced

## LinkedIn Login Attempts
- Multiple attempts with headless Chrome failed — LinkedIn bot detection blocks headless
- Xvfb non-headless also redirected to login page (credentials accepted but verification flow blocked)
- Credentials: alex.chen31337@gmail.com / LinkedInPass@2026l (stored encrypted)
- Status: STILL BLOCKED — needs manual browser login or LinkedIn API approach
