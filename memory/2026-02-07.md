# 2026-02-07 — EvoClaw Beta Day 🧬

## Massive Build Session (7 AM → ongoing)

### EvoClaw Beta — Built & Tested
5 parallel sub-agents on Opus 4.6 built the entire beta in ~1 hour:

1. **beta-release** — CI/CD, Docker, E2E integration tests, config templates
2. **beta-dashboard-docs** — Web dashboard (7 views, Bloomberg dark theme), 22 docs, llms.txt + llms-full.txt
3. **beta-trading** — Hyperliquid testnet, native Rust EIP-712 signing, paper trading, risk management, 247 tests
4. **beta-podman-edge** — Podman-first containers, bare metal Pi deployment, systemd services
5. **beta-e2b-cloud** — E2B sandbox integration, SaaS multi-tenant, cloud CLI

### Merge & Test
- All 5 branches merged into `beta` branch cleanly
- Fixed pre-existing race condition in evolution tests
- Go: 11 packages pass, Rust: 257 tests pass
- Tagged `v0.1.0-beta`

### Raspberry Pi 1 Deployment 🍓
- Cross-compiled Rust agent for ARMv6 (Pi 1)
- Switched to rustls (removed OpenSSL dependency)
- Static musl binary: **2.9MB**
- Deployed to Pi 1 (427MB RAM, single core BCM2835)
- Memory usage: **2.4MB** (0.5% of available RAM)
- Startup: instant (~3ms)

### Live Pipeline Running
- Mosquitto MQTT broker via Podman on PC
- Go orchestrator with web dashboard at :8420
- Pi agent connected and processing commands
- Dashboard showing real-time metrics (800+ messages processed)

### Key Fixes
- Wired agent MQTT reports → dashboard metrics (reports weren't updating UI)
- Fixed uptime display (was dividing by 1e9, API returns seconds)
- Fixed chart Y-axis label overlap
- Fixed LLM provider/model name parsing (strip `ollama/` prefix)
- Fixed MQTT broker address in join command (0.0.0.0 → hub IP)

### Deployment UX
- Built `evoclaw-agent join <HUB_IP>` command
- Auto-discovers hub, self-registers, writes config, starts agent
- Built `evoclaw setup hub` command
- Two-command deployment story:
  ```
  Server: evoclaw setup hub
  Pi:     evoclaw-agent join YOUR_SERVER_IP
  ```

### LLM Integration
- Wired Ollama (qwen2.5:1.5b) through orchestrator
- Pi agent sends query → orchestrator → Ollama → response in 2.88s
- Dashboard shows "Available Models: 1"

### Architecture Decisions
- Three-tier deployment: Edge (bare metal) / Server (Podman) / Cloud (E2B)
- Pi is the body, PC is the brain (LLM runs on PC, not Pi)
- Agent skill plugin system being built

### Market Monitoring
- Set up 4 cron jobs: KOL tweets, price alerts, market dashboard, Polymarket
- All on Opus 4.6, running every 30min-2h

### Stats
- ~25K lines of code + docs in one day
- 6+ sub-agents used
- 267 Rust tests + 11 Go packages all passing
- Zero downtime on the live Pi agent

### Bowen Context
- Woke up ~7 AM, excited about trading/ETH rebound
- Has old Raspberry Pi 1, testing edge deployment
- Wants LLM capabilities on Pi (via orchestrator proxy)
- Building toward agent skill plugin system
- Password: sudo pw is bowen@2025, Pi SSH is admin@192.168.99.25 pw 123456
