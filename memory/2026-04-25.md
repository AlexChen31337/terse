# Memory - 2026-04-25

## LinkedIn Posting — VERIFIED WORKING METHOD (2026-04-25 00:15 AEDT)

**Method:** Playwright CDP connect to Chrome (port 9222)

**Key steps:**
1. Connect: `await p.chromium.connect_over_cdp("http://127.0.0.1:9222")`
2. Find LinkedIn tab by URL containing `linkedin.com`  
3. Navigate to `https://www.linkedin.com/feed/`
4. Scroll to top: `await page.evaluate("window.scrollTo(0, 0)")`
5. Wait 2s for lazy load
6. Click share box using coordinates (NOT button selector):
   - Find: `div.share-creation-state__placeholder` at approximately (303, 175)
   - Click: `await page.mouse.click(x, y)` where x/y from `getBoundingClientRect()`
7. Wait 2s for modal to open
8. `page.locator('[contenteditable="true"]').first` — the editor
9. Type with human delays: char by char, `random.uniform(0.02, 0.08)` per char
10. Find Post button: `btns.find(b => b.textContent?.trim() === 'Post' && !b.disabled)`
11. Click via JS: `document.querySelectorAll('button').reverse().find(...).click()`
12. Verify: modal count == 0 after 5s = success

**DON'T use:**
- `button:has-text("Start a post")` — this is a DIV not a button
- `data-testid="share-box-trigger"` — doesn't exist
- browser_use Agent — Gemini rate limits + image_url not supported
- `fill()` directly — works on Twitter, NOT on LinkedIn (no events triggered)

**Verified:** Post appears on https://www.linkedin.com/in/alexchen31337/recent-activity/shares/
