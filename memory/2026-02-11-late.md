# 2026-02-11 Late Night Session Notes

## SadTalker + Bark TTS Setup (In Progress)
- Sub-agent timed out during PyTorch download (slow GPU server connection ~1.5MB/s)
- SadTalker repo cloned to `/home/peter/ai-stack/sadtalker/`
- Had to install `python3.12-venv` package (sudo password: from encrypted creds)
- **Switched to uv** per Bowen's request — uv installed at `/home/peter/.local/bin/uv`
- Old pip venv deleted, need to recreate with `uv venv` and install deps
- PyTorch install via uv still in progress (session `fresh-sable`)
- Still TODO: install SadTalker deps, download checkpoints, install Bark, create pipeline script, test e2e

## Image Generation Working
- ComfyUI + Juggernaut XL confirmed working on 3090
- Generated "Chinese lady on busy street" image successfully (street_lady_00001_.png)
- Sent to Bowen on WhatsApp
- Note: Python script quote escaping issue with heredoc — `'filename'` got interpreted. Image still generated, just error in polling script. Use double quotes or scp the file separately.

## Coverage Boost Sub-Agent Failed
- `evoclaw-boost-coverage` (Gemini model) burned 1M tokens outputting "(Action.)" repeated infinitely — completely broke
- Coverage still at ~55%, target is 85%+
- Need to retry with a different model (Claude, not Gemini)

## Video Generation Discussion
- Bowen asked about audio in video generation
- CogVideoX-5B, Wan2.1 — all video-only, no audio
- Sora/Movie Gen/V2A have audio but closed/unreleased
- Practical approach: video gen + separate audio (AudioLDM2 or MusicGen)
- For talking heads: SadTalker/Hallo2 + TTS (Bark/ElevenLabs) pipeline
- Bowen approved SadTalker + Bark setup

## Crypto Market
- BNB crossed -5% threshold (down 5.44% to $592.70)
- BTC ~$67K (-2.3%), ETH ~$1,953 (-2.6%), SOL ~$81 (-3.3%)
- Broad risk-off continues

## Key Reminders
- Use `uv` not pip for Python on GPU server (Bowen's preference)
- GPU server sudo password in encrypted creds file
- Beach storyboard video delivered successfully (5 clips, 31s, 1.8MB)
