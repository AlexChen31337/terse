# Memory: 2026-02-18

## Pre-Compaction Flush

### Infrastructure State (as of session end)

**GPU Server** (`peter@10.0.0.44`, sudo: `peter@2025`):
- Ollama v0.16.2, models dir: `/data2/ollama/models/` (env set in systemd)
- GLM-4.7-flash: 29.9B Q4_K_M, 19GB, ~21.7GB VRAM when loaded, **requires `think:false`**
- ComfyUI: PID 52093, `http://10.0.0.44:8188`, smart memory active, working dir `/data2/comfyui/ComfyUI/`
- Z-Image models all present: diffusion (bf16, 12.3GB), text encoder (qwen3_4b_fp8_mixed, 5.6GB), VAE (335MB)
- Root disk /dev/sda2: 109GB, ~88% full (14GB free) — monitor
- /data2: 916GB HDD, ~47% full (469GB free, 256GB swap here)
- GPUs: RTX 3090 (24GB, primary), RTX 3080 (10GB), RTX 2070 SUPER (8GB)

**EvoClaw orchestrator**: PID 2071496, port :8420, 2 agents (alex-hub, alex-eye both idle)
- Git head: `2a74384` — CI green (20 packages, -race, golangci-lint clean)
- Pi edge agent (`alex-eye`) hasn't heartbeated — likely disconnected

**OpenClaw**: Gateway running, Telegram channel active, update available (flagged by health check)

### Active Cron Jobs
- Crypto Price Alert (30min)
- Market Dashboard Summary (2h)
- Polymarket Odds Tracker (2h)
- Memory Consolidation Quick (4h)
- Daily workspace backup (3 AM)
- Memory Consolidation Daily (midnight)
- Memory Consolidation Monthly (1st)
- **Nightly Skill Health Check** — `ca76fb23-4d97-4061-88dd-91c24ca09c5e`, `0 0 * * *` AEDT, DeepSeek V3.2, 180s timeout

### Skills State
- **intelligent-router** v3.0 — enforced core skill, `ollama/glm-4.7-flash` as FREE SIMPLE primary
  - `scripts/spawn_helper.py --validate` exits 1 if no model in payload
  - AGENTS.md: mandatory model rule, Sonnet forbidden for monitoring
- **tiered-memory** — tree-based retrieval via `memory_cli.py retrieve`
- **agent-wal** — write-ahead log for corrections/decisions
- **agent-access-control** — WhatsApp set to `groupPolicy: allowlist`

### Pending Tasks
- [ ] Test GLM-4.7-flash tok/s: `curl http://10.0.0.44:11434/api/generate -d '{"model":"glm-4.7-flash","prompt":"What is 2+2?","think":false,"stream":true}'`
- [ ] Test Z-Image generation in ComfyUI (models ready, workflow not yet tested)
- [ ] Run `bash /home/bowen/clawd/skills/intelligent-router/install.sh` to self-patch AGENTS.md
- [ ] Fix Twitter/GitHub auth (`memory/twitter-bird-credentials.txt`, `memory/github-token.txt`)
- [ ] Debug alex-eye Pi agent connectivity
- [ ] Consider `ollama/glm-4.7-flash` for EvoClaw agents (currently using anthropic-proxy-6/glm-4.7)
- [ ] Monitor GPU root disk growth

### Key Lessons from Session
- **Ollama sparse file trap**: parallel-chunk downloads pre-allocate sparse file; renaming `-partial` before completion = zeroed model. Always wait for `ollama pull` to complete
- **GLM-4.7-flash thinking mode**: defaults to extended CoT; pass `"think":false` for all simple/monitoring tasks
- **TOOLS.md is infrastructure ground truth**: always check before diagnosing resource issues
- **CI protocol**: run `go test -race ./...` AND `golangci-lint run --timeout=5m` locally before every push
- **Cron model field mandatory**: no model = Sonnet default = expensive waste; validator enforces this
- **Nightly health-check self-referential timeout**: cron job listing times out because of self-reference — expected, not an error

### Security Hardening Done
- WhatsApp `groupPolicy` changed from `open` → `allowlist` (eliminates prompt injection from untrusted groups)
- Mandatory memory retrieval added to AGENTS.md
- Infrastructure diagnosis rules added to AGENTS.md (verify full spec before diagnosing)

---

## Session Continuation — Feb 18, 2026 (10:00–11:40 AM AEDT)

### EvoClaw CI Fixes
- Fixed 3 CI failures: golangci-lint `version` field not allowed, Rust Clippy (unused imports/fields/needless_return), cargo fmt
- CI now fully green: Go Lint ✅, Go Test ✅, Rust Lint ✅, Rust Test ✅, all builds ✅
- Added Android NDK + Windows edge agent targets to CI (aarch64-linux-android, armv7-linux-androideabi, x86_64-linux-android, x86_64-pc-windows-gnu)
- Added Android APK wrapper (Kotlin foreground service) + Termux install script
- Added Gradle build to CI — produces debug APK artifact on every push

### ClawChain ADRs — Closed Out
- All 10 architecture decisions (issues #13–16, #19–23, #25) closed with ADR summary posted on #24
- ADR-009 amended: Nostr WoT API explicitly rejected as validator trust dependency — use pallet-reputation + staking on-chain only
- Bowen confirmed: gas fee approach correct (hybrid stake-based quota + fees, not pure zero-gas)

### ClawChain Q2 Implementation Kickoff
- Discovered full Substrate code is on VPS at /root/claw-chain (NOT docs-only as I incorrectly said)
- 19 pallets deployed on testnet.clawchain.win: System, Timestamp, Aura, Grandpa, Balances, TransactionPayment, Authorship, Session, Historical, Staking, Offences, BagsList, Treasury, Sudo, AgentRegistry, ClawToken, Reputation, TaskMarket, RpcRegistry
- VPS has no git credentials — sync goes VPS → local → GitHub
- Synced rpc-registry pallet + full runtime to GitHub (commit 214cb6b)
- Created pallet-gas-quota (ADR-002): 364 lines, 8 tests, stake-based daily quota (commit 840dc29)
- Fixed type error: saturating_pow expects usize not u32 (commit 36cae91)
- Created 10 Q2 implementation GitHub issues (#27–36) + milestone "Q2 2026 Implementation"
- Updated ROADMAP.md with Q1 complete + ADR table

### ClawChain Runtime Upgrade (IN PROGRESS)
- Gas-quota pallet copied to VPS, wired into runtime Cargo.toml + lib.rs
- Build started on VPS (PID 207852) with proper PATH — confirmed compiling at 11:37 AM
- One-shot cron scheduled for 12:05 PM to check build + submit WASM upgrade via sudo

### Crypto Price Alert Fixed
- CoinGecko was 403-blocking + no Brave Search key
- Switched to Binance public API (no key needed) — working immediately
- Now reports BTC/ETH/SOL/BNB/XRP with 30m klines for movement detection

### Skills Assessment
- Reviewed openclaw/openclaw skills repo
- Recommended for installation: coding-agent, sag (ElevenLabs TTS), session-logs, summarize, blogwatcher, model-usage
- Bowen confirmed: yes to installing those

### Key Decisions Today
- No Nostr WoT API for validator trust — use pallet-reputation on-chain
- 0-gas fee rejected — keep hybrid stake-based quota (spam protection)
- VPS code sync protocol: always push from local machine (VPS has no git creds)
- Build script needs `export PATH=$HOME/.cargo/bin:$PATH` when using nohup on VPS

### MEMORY.md Correction
- Fixed: "8 pallets deployed" → "19 pallets deployed"
- Fixed: ClawChain status now correctly shows wss:// WebSocket-only endpoint

---

## Second Flush (afternoon session)

### GLM-4.7-flash — FULLY OPERATIONAL
- **Root cause of slow start**: Ollama 0.16.2 `--ollama-engine` needs CUDA kernel compilation on first run (~12 min for 30B MoE)
- **Fix**: Set `OLLAMA_LOAD_TIMEOUT=1200` in `/etc/systemd/system/ollama.service.d/override.conf`
- **Performance confirmed**: 85 tok/s generation, 306 tok/s prompt processing
- **Gateway registered**: New provider `ollama-gpu-server` pointing to `http://10.0.0.44:11434/v1`, alias `LocalGLM47Flash`
- **Intelligent Router updated**: Added to MEDIUM fallback chain, upgraded capabilities (code, function-calling, bilingual, agentic=true)
- **Use in cron jobs**: `"model": "ollama-gpu-server/glm-4.7-flash"` with `"think": false` for SIMPLE tasks

### Z-Image — Still Stuck
- All 3 models detected by ComfyUI ✅ (diffusion, VAE, qwen_image text encoder)
- Workflow submitted with `type: "qwen_image"` for CLIPLoader ✅
- **Problem**: 12.3GB bf16 diffusion model won't load — ComfyUI needs >12GB system RAM to stage it, only 6GB available
- **Attempted fix**: Restarted ComfyUI with `--gpu-only` flag (bypasses RAM staging, loads direct to VRAM)
- **Status**: Job `070d7dc4` queued and loading as of 1:15 PM — outcome unknown (session compacted)
- **GPU server RAM**: 16GB total, ComfyUI uses ~10GB, leaves ~6GB — insufficient for 12.3GB model staging
- **Next session**: Check if `--gpu-only` fixed it; if not, consider fp16 quantization or `--lowvram` mode

### ClawChain Testnet
- **pallet-gas-quota (ADR-002)** deployed to testnet via `sudo.sudo_unchecked_weight(System::set_code(...))` ✅
- **Runtime upgrade**: Spec version now 100 on testnet
- **Systemd service created**: `/etc/systemd/system/clawchain.service` — auto-starts on reboot, restart on failure ✅
- **Node running**: PID 211581, block ~#75+, `journalctl -u clawchain` for logs
- **Node flags**: `--dev --base-path /root/.clawchain/testnet --rpc-cors all --rpc-methods unsafe --rpc-external --rpc-port 9944`
- **pallet-agent-did (ADR-003)**: Sub-agent spawned to implement (OAuth Sonnet, v2 attempt after Proxy 2 quota exhausted)

### qwen2.5:7b Download
- Downloading on GPU server for parallel GPU test (~4.7GB, ~31% at last check, ~8 min ETA)
- Purpose: Load on GPU 1 (RTX 3080, 10GB) while GLM-4.7-flash on GPU 0 (RTX 3090)
- Tests multi-GPU parallel model serving via Ollama

### Ollama Multi-GPU Config
- `OLLAMA_GPU_MULTI_GPU=1` already in override.conf ✅
- Works correctly — auto-selects single GPU when model fits (GLM on GPU 0 only)
- With qwen2.5:7b on GPU 1: total 42GB VRAM pool across 3 GPUs
- PCIe (not NVLink) so cross-GPU adds latency — single GPU preferred when it fits

### Mainnet Roadmap Assessment
- Phase 1: pallet-agent-did (#30) + pallet-quadratic-governance (#31) + PoA chain spec (#28)
- Phase 2: TypeScript SDK (#32) + Faucet (#33) + Explorer (#34)
- Phase 3: Security audit + benchmarks
- Phase 4: Mainnet genesis + launch
- Estimate: 6-8 weeks at current pace
- **Blocker**: Node still runs `--dev` (Alice+Bob test keys). `local_testnet_config()` already exists in node/src/chain_spec.rs — switch is straightforward

### Crypto Market (today)
- Fear & Greed: 8 (Extreme Fear)
- BTC: ~$67.3K (-2.5%), ETH: ~$1,984 (-1%), SOL: ~$84.7 (-3.2%)
- SOL crossed -3% alert threshold at 12:22 PM
- Abu Dhabi wealth funds crossed $1B BTC ETF; Dragonfly raised $650M VC fund
- Markets correlating with Nasdaq tech selloff

### WhatsApp Connected
- WhatsApp gateway reconnected at 14:15 PM
