[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "arxiv-to-code"
version = "0.1.0"
description = "Automated pipeline: fetch arxiv papers → generate skeleton implementations → push to GitHub"
requires-python = ">=3.10"
dependencies = [
    "httpx>=0.27.0",
    "feedparser>=6.0.11",
    "PyGithub>=2.1.1",
    "arxiv>=2.1.0",
]

[project.scripts]
arxiv-to-code = "arxiv_to_code.pipeline:main"
